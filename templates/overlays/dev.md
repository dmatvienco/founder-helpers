# Developer — project overlay

This file belongs to YOUR project's developer. The shipped template carries the generic
workflow (one issue per run, branch discipline, checks, honest reports); everything
project-specific lands here. The team edits this file itself as it learns. Commit it.

## Environment gotchas

<!-- Hard-won, machine-specific truths. Examples from the project this tool grew out
     of: "never run gradlew clean — it breaks the native build", "npm run android
     returns non-zero AFTER a successful install — verify by adb, not exit code".
     Every hour lost to the environment should become a line here. -->

## Build, test and smoke procedures

<!-- Anything beyond the checks configured in config.json: emulators, devices,
     E2E suites, how to capture screenshots for the PM. -->

## Repo conventions

<!-- Commit style, code patterns to follow, directories to avoid touching. -->

## Lessons learned

<!-- Corrections from the founder or the reviewer, with dates and the WHY. -->
