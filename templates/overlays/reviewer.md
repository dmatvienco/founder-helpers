# Reviewer — project overlay

This file belongs to YOUR project's reviewer. The shipped template carries the generic
review discipline; the stack-specific knowledge that makes reviews sharp lands here.
The team edits this file itself as it learns. Commit it.

## Stack patterns to review against

<!-- e.g. "frontend: Redux Toolkit + React Query — flag ad-hoc fetch calls",
     "backend: gin + sqlc — flag hand-written SQL outside the queries file". -->

## How to verify claims

<!-- Test commands worth rerunning when in doubt, and how long they take. -->

## Known trouble spots

<!-- Modules where regressions historically hide; things that MUST be checked
     whenever touched (migrations, API contracts with released clients...). -->
